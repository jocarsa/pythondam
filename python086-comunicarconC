#include <stdio.h>
int main(int argc, char** argv) {    
    FILE *archivo;    
    archivo = fopen(«prueba.txt»,»w»);    
    fprintf(archivo,»Esto es una prueba \n»);    
    fclose(archivo);    
    return 0;
}